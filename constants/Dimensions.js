import { Dimensions } from 'react-native';

export const deviceHeight = Dimensions.get('screen').height.toFixed(); // string
export const deviceWidth = Dimensions.get('screen').width.toFixed(); // string
